#include <iostream>
#include<string>

using namespace std;
int main() 
{
  string str1, str2;
  while (cin>>strl>>str2)
  {
    int count = 0;
    for (size_t i=0; (i-str1.find(str2,i)) != str1.npos; i+=str2.size ())
      count++;
    cout<<count<<endl;
   }

    return 0;
}
